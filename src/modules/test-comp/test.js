const snake = (size) => {
  let matrix = Array.from(new Array(size), () => Array.from(new Array(size), () => 0));
  if (size < 3) {
    return matrix;
  }


};

const walkStep = (matrix) => {
  if (matrix.length < 3) {

  }
}
